<% @announcements.each do |announcement| %>
	<div class="demo-card-wide mdl-card mdl-shadow--2dp list-card">
      	<div class="mdl-card__supporting-text">
    		<img src="http://www.panamericanworld.com/sites/default/files/jose-bautista-beard-4.jpg" class="image"/>
        	<% if announcement.importance == "1" %> <br>
                <i class="material-icons">error</i> <%= announcement.description %> <br><br>
            <% else %>
                <%= announcement.description %> <br><br>
            <% end %>

            <div class="delete-button">

            <%= link_to('<i class="material-icons">delete</i>'.html_safe, house_announcement_path(current_user.house_id, announcement), method: :delete) if announcement.owner_id == current_user.id %>
            </div>
        </div>
    </div>
<% end %>


