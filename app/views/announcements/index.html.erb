<h1> What's Happenin'? </h1>

<div class="description">
  <p> Announcements go here </p>
</div>

<div class="new-announcement">
  <%= render "new_announcement_modal" %>
</div>

<div id="announcement-list">
  <%= render "announcement_list" %>
  <p> <%= link_to "Send text", house_text_sender_path(current_user.house_id), method: :patch %> </p>
</div>


<%= paginate @announcements %>
